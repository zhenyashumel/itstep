from tkinter import *

root=Tk() - Главное окно
root.mainloop() - Метод ожидания события
root.title("Your_title") - название окна
root.geometry("500x500") - размер окна (строковое значение ввода)
Расширение - .pyw - без консоли

but=Button(root1) --создает кнопку
but["text"]="Печать" -- текст кнопки
but.bind("<Button-1>",printer) --выбор кнопки и выбор действия (Button-1 левая кн.мыши, Button-2 -колесико,Button-3 - правая кн.мыши
but.pack() -- отображение кнопки
width =10 ширина
height= 5 -высота
bg="orange" -цвет фона
fg="blue" -цвет теста

Label -метка(просто вывод текста)

Entry -однострочное текстовое поле (bd -размер рамки) 
Text (шинира , высота, шрифт, wrap=(перемещение по строкам)(NONE -по интеру, WORD-по словам, CHAR - по буквам)

#радиокнопки
var=IntVar() 
var.set(1) -выделение кнопки при запуске
rad0= radiobutton(root, text, variable=var,value=0) (value- номер кнопки(с нуля))

с1=IntVar()
che1=Checkbutton(root,text,variable=c1,onvalue=1,offvalue=0)


#списки
r = ['Linux', 'Python', 'Tk', 'Tkinter','Igor']
lis = Listbox(root, selectmode=EXTENDED, height=5,bd=)



#рамки

Frame(root,ширина,высота,цвет фона)

#Рамка в рамке
fra2 = Frame(root,
             width = 300,
             height = 200,
             bg = "green",
             bd = 50)
             
fra4 = Frame(fra2,
             width = 200,
             height = 100,
             bg = 'gray',
             bd = 25)
fra4.pack()

f_4.pack(side=LEFT) -параметр размещения, fill=y -растянуть по оси y

#Строка в рамке
ent1 = Entry(fra4, width = 20)
ent1.pack() 
#шкалы
Scale(orient(горизонт,верт), длина, откуда до куда,tickintervalresolution=(интервал)


scr = Scrollbar(root,
                command=tx.yview) -yview и xview (по оси х и оси у)
tx.configure(yscrollcommand=scr.set)
 
tx.grid(row=0,column=0)
scr.grid(row=0,column=1)


#дочернее окно

win = Toplevel(root,
               relief=SUNKEN,
               bd=10,
               bg="lightblue")
win.title("Дочернее окно")
win.minsize(width=400,
            height=200)
win.grid()
win.mainloop()
root.mainloop()

l_num.grid(row=1,column=0) - менеджер размещения. (как таблица(row-строка,column - столбик)
columnspan=2 (объединение по столбикам)
rowspan=2 (объединения по строкам)
padx=50, pady=50 (выравнивание по x и y)
root.destroy() -выход








