#include <iostream>
 
using namespace std;
 
int divs(int n, int k)
{
   
    if (n == 1)
        return 0;
    return (k == 1) ? 0 : divs(n, k - 1) + (n % k == 0);
}
 
int main (void)
{
   
    int n;
    cin >> n;
    cout << divs (n, n / 2);
    return 0;
   
}
RAW Paste Data
